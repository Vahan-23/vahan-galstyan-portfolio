/**
 * Testimonials section — intentionally empty.
 * Hidden until real quotes exist. Do not invent testimonials.
 *
 * {{TODO: add real quotes, then render this section on the homepage}}
 */
export function Testimonials() {
  const testimonials: never[] = [];

  if (testimonials.length === 0) {
    return null;
  }

  return null;
}
